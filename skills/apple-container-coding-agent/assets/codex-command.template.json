{
  "argv": [
    "codex",
    "exec",
    "--ephemeral",
    "--sandbox",
    "workspace-write",
    "--ignore-user-config",
    "--ignore-rules",
    "--json",
    "/run/task-contract.jsonに従う。private workspaceだけを編集する。Git remoteを設定・使用しない。未解決事項を明示する。"
  ]
}
