# Tests

Run the skill's standard-library-only tests from the repository root:

```bash
python3 -m unittest discover -s skills/apple-container-validation/tests -p 'test_*.py'
```

These tests do not start Apple Container.
